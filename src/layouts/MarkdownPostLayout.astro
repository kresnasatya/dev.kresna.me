---
import PostLanguageSwitcher from '../components/PostLanguageSwitcher.astro';
import Toc from '../components/Toc.astro';
import Layout from "./Layout.astro";

const { frontmatter, headings } = Astro.props;
---

<Layout title={frontmatter.title} description={frontmatter.description}>
    <main>
        <h1 style="text-align: left; font-size: 2rem;">{frontmatter.title}</h1>
        <span style="color: gray;">{(new Intl.DateTimeFormat('en-US', { dateStyle: 'full' })).format(new Date(frontmatter.pubDate))}</span>
        |
        <PostLanguageSwitcher currentId={frontmatter.id}/>
        <Toc headings={headings}/>
        <slot />
    </main>
</Layout>

<style>
    main :global(h2) {
        font-size: 1.75rem;
    }

    main :global(h3) {
        font-size: 1.5rem;
        margin-top: 1rem;
        margin-bottom: 1rem;
    }

    main :global(p) {
        margin-top: 1rem;
        margin-bottom: 1rem;
        font-size: 1.25rem;
    }

    main :global(ol), main :global(ul) {
        font-size: 1.25rem;
    }

    main :global(blockquote) {
        font-style: italic;
        margin: 0;
        border-left: 2px solid gray;
        padding-left: .5rem;
        color: rgb(20, 29, 40);
    }

    main :global(img) {
        width: 100%;
        height: auto;
        aspect-ratio: 16/9;
        display: block;
    }

    main :global(pre) {
        padding: 1rem;
    }

    main :global(code) {
		font-family:
			Menlo,
			Monaco,
			Lucida Console,
			Liberation Mono,
			DejaVu Sans Mono,
			Bitstream Vera Sans Mono,
			Courier New,
			monospace;
		font-size: 1rem;
		background-color: rgb(237, 238, 243);
		padding: .25rem;
		color: orangered;
	}

	main :global(pre) {
		margin-bottom: 1rem;
	}

	main :global(pre > code) {
		background-color: initial;
		padding: initial;
		color: initial;
	}

    main :global(.rcg-tab-container) {
        overflow-x: auto;
    }
</style>